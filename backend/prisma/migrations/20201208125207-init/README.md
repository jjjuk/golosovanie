# Migration `20201208125207-init`

This migration has been generated by jjjuk at 12/8/2020, 8:52:07 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "EventName" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "name" TEXT NOT NULL
)

PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Event" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "eventNameId" INTEGER NOT NULL,
    "approved" BOOLEAN NOT NULL DEFAULT false,
    "startTime" TEXT NOT NULL,

    FOREIGN KEY ("eventNameId") REFERENCES "EventName"("id") ON DELETE CASCADE ON UPDATE CASCADE
);
INSERT INTO "new_Event" ("id", "approved", "startTime") SELECT "id", "approved", "startTime" FROM "Event";
DROP TABLE "Event";
ALTER TABLE "new_Event" RENAME TO "Event";
CREATE TABLE "new_Poll" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "createdAt" TEXT NOT NULL,
    "currentStage" INTEGER NOT NULL DEFAULT 1,
    "active" BOOLEAN NOT NULL DEFAULT true,
    "userId" INTEGER NOT NULL,
    "firstStageTime" TEXT NOT NULL,
    "secondStageTime" TEXT NOT NULL,

    FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE
);
INSERT INTO "new_Poll" ("id", "createdAt", "active", "userId", "firstStageTime", "secondStageTime") SELECT "id", "createdAt", "active", "userId", "firstStageTime", "secondStageTime" FROM "Poll";
DROP TABLE "Poll";
ALTER TABLE "new_Poll" RENAME TO "Poll";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON

CREATE UNIQUE INDEX "EventName.name_unique" ON "EventName"("name")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201208122400-init..20201208125207-init
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -20,19 +20,26 @@
 model Poll {
   id Int @default(autoincrement()) @id
   createdAt String
+  currentStage Int @default(1)
   active Boolean @default(true)
   userId Int
   user User @relation(fields: [userId], references: [id])
   firstStageTime String
   secondStageTime String
   votes Vote[]
 }
+model EventName{
+  id Int @default(autoincrement()) @id
+  name String @unique
+}
+
 model Event {
   id Int @default(autoincrement()) @id
-  name String
+  eventNameId Int
+  eventName EventName @relation(fields: [eventNameId], references: [id])
   approved Boolean @default(false)
   startTime String
 }
```


